<h1>Tasks</h1>
<%= truncate("A text that is long long far away in the far far away galaxy of nothingness") %>
<%= link_to "New task", new_task_path %>

<div id="tasks">
  <% @tasks.each do |task| %>
    <div>
      <%= link_to task.name, task %>>
    </div>
  <% end %>
</div>
